<?xml version="1.0" encoding="UTF-8"?>
<domain-models xmlns="http://axelor.com/xml/ns/domain-models"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://axelor.com/xml/ns/domain-models http://axelor.com/xml/ns/domain-models/domain-models_4.1.xsd">

  <module name="bank-payment" package="com.axelor.apps.bank.payment.db"/>

  <entity name="BankOrderEconomicReason" lang="java">
  
	<string name="code" title="Code" min="3" max="3"/>
	<string name="label" title="Label"/>
	<string name="description" title="Description" large="true" multiline="true"/>
	<string name="name" title="Name">
		<![CDATA[
		String name = "";
		if(code != null && !code.isEmpty())  {
			name = code + " - " + label;
		}
    	if(label != null && !label.isEmpty()) {
    		if(!name.isEmpty())  {
		    	name += " - ";
		    }
		    name += label;
	    }
		return name;
	    ]]>
	</string>
  	
  </entity>

</domain-models>
