---
title: "Sale order: use AppSaleService to fetch AppSale configuration."
module: axelor-production
developer: |
  In `SaleOrderLineBomSyncServiceImpl`, replaced `AppSaleRepository` with `AppSaleService` in the constructor.
