---
title: "Invoice/InvoiceTerm : fixed the amount init when manually splitting amounts."
module: axelor-account
developer :
  The InvoiceTermServiceImpl.computePercentageSum method now return the amount without scale to be used in computation.
  If you want to scale it, use the currencyScaleService.getScaledValue(invoice, amount)
