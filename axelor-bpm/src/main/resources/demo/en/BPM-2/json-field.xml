<?xml version="1.0" encoding="utf-8"?>
<jason-fields>

  <json-field>

    <name>panel1</name>

    <title></title>

    <type>panel</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>0</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 12, "validIf": null,
      "itemSpan": 6, "collapseIf": "", "canCollapse": "false"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>Pizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>panel1</name>

    <title></title>

    <type>panel</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>0</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 12, "validIf": null,
      "itemSpan": 6, "collapseIf": "", "canCollapse": "false"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>CalmCustomer</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>name</name>

    <title>Name</title>

    <type>string</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>true</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>2</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 2, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>panel1</name>

    <title></title>

    <type>panel</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>0</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 12, "validIf": null,
      "itemSpan": 6, "collapseIf": "", "canCollapse": "false"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>pizza</name>

    <title>Pizza</title>

    <type>json-many-to-one</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>3</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel>Pizza</targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>name</name>

    <title>Name</title>

    <type>string</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>1</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>Pizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>name</name>

    <title>Name</title>

    <type>string</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>1</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>CalmCustomer</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>pizzaOrder</name>

    <title>Pizza order</title>

    <type>json-many-to-one</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>2</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 2, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>CalmCustomer</jsonModel>

    <targetJsonModel>PizzaOrder</targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>statusSelect</name>

    <title>Status select</title>

    <type>integer</type>

    <defaultValue>1</defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection>custom-json-select-PizzaOrder-statusSelect</selection>

    <sequence>1</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget>NavSelect</widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": "12", "showTitle":
      "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>PizzaOrder</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText>1:New
      2:Bake the pizza
      3:Delivery the pizza
      4:Receive payment
    </selectionText>

  </json-field>
  <json-field>

    <name>panel1</name>

    <title></title>

    <type>panel</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>0</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 12, "validIf": null,
      "itemSpan": 6, "collapseIf": "", "canCollapse": "false"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>AskPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>name</name>

    <title>Name</title>

    <type>string</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>1</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>AskPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>hungryPizza</name>

    <title>Hungry pizza</title>

    <type>json-many-to-one</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>2</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 2, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>AskPizza</jsonModel>

    <targetJsonModel>HungryPizza</targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>statusSelect</name>

    <title>Status</title>

    <type>integer</type>

    <defaultValue>1</defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection>custom-json-select-HungryPizza-statusSelect</selection>

    <sequence>1</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget>NavSelect</widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": "12", "showTitle":
      "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText>1:New
      2:Select pizza
      3:Order pizza
      4:Pay pizza
      5:Eat the pizza
    </selectionText>

  </json-field>
  <json-field>

    <name>panel1</name>

    <title></title>

    <type>panel</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>0</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 12, "validIf": null,
      "itemSpan": 6, "collapseIf": "", "canCollapse": "false"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>PizzaOrder</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>orderBy</name>

    <title>Order by</title>

    <type>many-to-one</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick></onClick>

    <precision>6</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>2</scale>

    <selection></selection>

    <sequence>5</sequence>

    <showIf></showIf>

    <targetModel>com.axelor.auth.db.User</targetModel>

    <widget></widget>

    <widgetAttrs>{"colSpan": 6}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>orderDate</name>

    <title>Order date</title>

    <type>date</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick></onClick>

    <precision>6</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>2</scale>

    <selection></selection>

    <sequence>6</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"colSpan": 6}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>pizzaOrder</name>

    <title>Pizza order</title>

    <type>json-many-to-one</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>true</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>4</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"colSpan": 6}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel>PizzaOrder</targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>payPizzaBtn</name>

    <title>Pay Pizza</title>

    <type>button</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>true</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save,action-refresh-record</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>7</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>name</name>

    <title>Name</title>

    <type>string</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>true</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>true</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>2</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"colSpan": 6}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>PizzaOrder</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>bakePizzaBtn</name>

    <title>Bake the pizza</title>

    <type>button</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>true</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save,action-refresh-record</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>6</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": "3", "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>PizzaOrder</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>deliverPizzaBtn</name>

    <title>Deliver pizza</title>

    <type>button</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>true</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save,action-refresh-record</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>7</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 2, "tab": "false", "width": "default", "colSpan": "3", "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>PizzaOrder</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>receivePaymentBtn</name>

    <title>Receive Payment</title>

    <type>button</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>true</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save,action-refresh-record</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>8</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": "3", "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>PizzaOrder</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>orderPizzaBtn</name>

    <title>Order pizza</title>

    <type>button</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>true</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save,action-refresh-record</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>8</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 2, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>eatPizzaBtn</name>

    <title>Eat pizza</title>

    <type>button</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>true</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick>save,action-refresh-record</onClick>

    <precision>0</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>0</scale>

    <selection></selection>

    <sequence>9</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"col": 1, "tab": "false", "width": "default", "colSpan": 6, "showTitle": "true"}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>HungryPizza</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>spacer24</name>

    <title></title>

    <type>spacer</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick></onClick>

    <precision>6</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>2</scale>

    <selection></selection>

    <sequence>3</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"colSpan": 6}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>PizzaOrder</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>orderBy</name>

    <title>Order by</title>

    <type>many-to-one</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick></onClick>

    <precision>6</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>2</scale>

    <selection></selection>

    <sequence>4</sequence>

    <showIf></showIf>

    <targetModel>com.axelor.auth.db.User</targetModel>

    <widget></widget>

    <widgetAttrs>{"colSpan": 6}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>PizzaOrder</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
  <json-field>

    <name>orderDate</name>

    <title>Order date</title>

    <type>date</type>

    <defaultValue></defaultValue>

    <domain></domain>

    <enumType></enumType>

    <help></help>

    <formView></formView>

    <gridView></gridView>

    <hidden>false</hidden>

    <visibleInGrid>false</visibleInGrid>

    <hideIf></hideIf>

    <maxSize>0</maxSize>

    <minSize>0</minSize>

    <model>com.axelor.meta.db.MetaJsonRecord</model>

    <modelField>attrs</modelField>

    <nameField>false</nameField>

    <onChange></onChange>

    <onClick></onClick>

    <precision>6</precision>

    <regex></regex>

    <required>false</required>

    <requiredIf></requiredIf>

    <readonly>false</readonly>

    <readonlyIf></readonlyIf>

    <includeIf></includeIf>

    <scale>2</scale>

    <selection></selection>

    <sequence>5</sequence>

    <showIf></showIf>

    <targetModel></targetModel>

    <widget></widget>

    <widgetAttrs>{"colSpan": 6}</widgetAttrs>

    <valueExpr></valueExpr>

    <jsonModel>PizzaOrder</jsonModel>

    <targetJsonModel></targetJsonModel>

    <appBuilder></appBuilder>

    <modelAppBuilder>BPM-2</modelAppBuilder>

    <isSelectionField>false</isSelectionField>

    <selectionText></selectionText>

  </json-field>
</jason-fields>