<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<object-views xmlns="http://axelor.com/xml/ns/object-views"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://axelor.com/xml/ns/object-views http://axelor.com/xml/ns/object-views/object-views_8.1.xsd">

  <action-record model="com.axelor.apps.purchase.db.PurchaseOrder"
    name="action.purchase.chart.set.date.6m">
    <field name="toDate" expr="eval:LocalDate.parse(todayDate)"/>
    <field name="fromDate" expr="eval:LocalDate.parse(todayDate).minusMonths(6)"/>
    <field name="monthSelect" expr="eval:6"/>
  </action-record>

  <action-record model="com.axelor.apps.purchase.db.PurchaseOrder"
    name="action.purchase.chart.set.date">
    <field name="toDate" expr="eval:LocalDate.parse(todayDate)"/>
    <field name="fromDate" expr="eval:LocalDate.parse(todayDate).minusYears(1)"/>
    <field name="monthSelect" expr="eval:12"/>
  </action-record>

  <chart onInit="action.purchase.chart.set.date.6m"
    name="chart.my.purchase.per.product.family.volume"
    title="My purchased qty by product accounting family">
    <search-fields>
      <field type="date" name="fromDate" title="From Date" x-required="true"/>
      <field type="date" name="toDate" title="To Date" x-required="true"/>
    </search-fields>
    <dataset type="jpql">

      SELECT
      SUM(self.qty) AS _qty,
      product.productFamily.name AS _product_family
      FROM
      PurchaseOrderLine self,
      Product product

      WHERE
      self.product = product
      AND
      DATE(self.purchaseOrder.validationDateTime) &gt;= DATE(:fromDate)
      AND
      DATE(self.purchaseOrder.validationDateTime)
      &lt;=
      DATE(:toDate)
      AND
      self.purchaseOrder.statusSelect &gt; 3
      AND self.purchaseOrder.statusSelect &lt;
      6
      AND
      self.purchaseOrder.buyerUser = :__user__
      AND self.purchaseOrder.company = :activeCompany
      GROUP
      BY
      product.productFamily.name
      ORDER BY
      product.productFamily.name

    </dataset>
    <category key="_product_family" type="text" title="Product Accounting Family"/>
    <series key="_qty" type="donut" title="Quantity"/>
  </chart>


  <chart onInit="action.purchase.chart.set.date.6m"
    name="chart.my.purchase.amount.per.product.family"
    title="My purchased amount by product accounting family">
    <search-fields>
      <field type="date" name="fromDate" title="From Date" x-required="true"/>
      <field type="date" name="toDate" title="To Date" x-required="true"/>
    </search-fields>
    <dataset type="jpql">

      SELECT
      SUM(self.exTaxTotal) AS amount,
      product.productFamily.name AS
      _product_family
      FROM
      PurchaseOrderLine self,
      Product product

      WHERE
      self.purchaseOrder.buyerUser =
      :__user__
      AND self.purchaseOrder.company = :activeCompany
      AND
      DATE(self.purchaseOrder.validationDateTime)
      &gt;= DATE(:fromDate) AND
      DATE(self.purchaseOrder.validationDateTime)
      &lt;=
      DATE(:toDate)
      AND
      self.purchaseOrder.createdOn
      &lt;= CURRENT_TIMESTAMP
      AND
      self.purchaseOrder.statusSelect IN
      (4,5)
      AND self.product = product
      GROUP BY
      product.productFamily.name
      ORDER BY
      product.productFamily.name

    </dataset>
    <category key="_product_family" type="text" title="Product Accounting Family"/>
    <series key="amount" type="bar" title="Amount"/>
  </chart>


  <chart onInit="action.purchase.chart.set.date.6m"
    name="chart.purchase.order.volume.by.buyer.by.family"
    title="POs Volume by buyer by accounting family">

    <search-fields>
      <field name="fromDate" type="date" title="From Date" x-required="true"/>
      <field name="toDate" type="date" title="To Date" x-required="true"/>
    </search-fields>

    <dataset type="sql">

      select
      sum(_line.qty) as qty,
      buyer.full_name buyer_name,
      family.name as
      family_name
      FROM
      purchase_purchase_order_line _line
      JOIN
      purchase_purchase_order self
      ON
      _line.purchase_order=self.id
      JOIN
      auth_user _user
      ON _user.id = :__user__
      AND self.company =
      _user.active_company
      JOIN
      auth_user buyer
      ON buyer.id=self.buyer_user
      JOIN
      base_product product
      ON
      product.id=_line.product
      JOIN
      base_product_family family
      ON product.product_family=family.id
      where
      DATE(self.validation_date_time) &gt;= DATE(:fromDate) AND DATE(self.validation_date_time)
      &lt;= DATE(:toDate)
      AND self.status_select in(4,5)
      group by buyer_name, family_name

    </dataset>
    <category key="buyer_name" type="text" title="Buyer"/>
    <series key="qty" type="bar" title="Quantity" groupBy="family_name"/>
  </chart>


  <chart onInit="action.purchase.chart.set.date.6m"
    name="chart.purchase.order.amount.by.family" title="Purchased amount by accounting family">
    <search-fields>
      <field name="fromDate" type="date" title="From Date" x-required="true"/>
      <field name="toDate" type="date" title="To Date" x-required="true"/>
    </search-fields>

    <dataset type="sql">

      SELECT
      family.name as family_name,
      coalesce(sum(self.ex_tax_total) ,0) as
      amount
      FROM
      purchase_purchase_order_line orders
      JOIN
      purchase_purchase_order self
      ON
      self.id=orders.purchase_order
      JOIN
      auth_user _user
      ON _user.id = :__user__
      AND self.company =
      _user.active_company
      JOIN
      base_product product
      ON product.id=orders.product
      JOIN
      base_product_family family
      ON product.product_family=family.id
      WHERE
      self.status_select in(4,5)
      AND DATE(self.validation_date_time) &gt;= DATE(:fromDate) AND DATE(self.validation_date_time)
      &lt;=
      DATE(:toDate)
      GROUP BY
      family_name

    </dataset>
    <category key="family_name" type="text" title="Product Accounting Family"/>
    <series key="amount" type="bar" title="Amount"/>
  </chart>


  <chart onInit="action.purchase.chart.set.date.6m"
    name="chart.purchase.order.amount.distribution.by.family"
    title="Purchased amount distribution by accounting family">
    <search-fields>
      <field name="fromDate" type="date" title="From Date" x-required="true"/>
      <field name="toDate" type="date" title="To Date" x-required="true"/>
    </search-fields>

    <dataset type="sql">

      SELECT

      base_product_family.name AS _family_product,
      TRUNC((SUM(purchase_purchase_order_line.ex_tax_total) * 100)/(SELECT
      SUM(purchase_purchase_order_line.ex_tax_total)
      FROM
      purchase_purchase_order self
      JOIN
      auth_user
      _user
      ON _user.id = :__user__
      AND self.company = _user.active_company
      LEFT JOIN
      purchase_purchase_order_line ON purchase_purchase_order_line.purchase_order = self.id
      WHERE
      DATE(self.validation_date_time) &gt;= DATE(:fromDate) AND DATE(self.validation_date_time)
      &lt;= DATE(:toDate)
      AND
      self.status_select IN (4,5)),2) AS _percent
      FROM
      purchase_purchase_order
      self
      JOIN
      auth_user _user
      ON _user.id = :__user__
      AND self.company = _user.active_company
      LEFT JOIN
      purchase_purchase_order_line ON purchase_purchase_order_line.purchase_order = self.id
      LEFT JOIN
      base_product ON base_product.id = purchase_purchase_order_line.product
      LEFT JOIN
      base_product_family ON base_product_family.id = base_product.product_family
      WHERE
      DATE(self.validation_date_time) &gt;= DATE(:fromDate) AND DATE(self.validation_date_time)
      &lt;= DATE(:toDate)
      AND
      self.status_select IN (4,5)
      GROUP BY
      base_product_family.name
      ORDER BY
      base_product_family.name

    </dataset>
    <category key="_family_product" type="text" title="Product Accounting Family"/>
    <series key="_percent" type="donut" title="Percent"/>
  </chart>

  <chart onInit="action.purchase.chart.set.date" name="chart.total.purchase.amount"
    title="Total Purchased Amount by month">
    <search-fields>
      <field type="date" name="fromDate" title="From Date" x-required="true"/>
      <field type="date" name="toDate" title="To Date" x-required="true"/>
    </search-fields>
    <dataset type="sql">
      SELECT
      to_char(_order.validation_date_time,'yyyy-MM') AS _month,
      SUM(_order.ex_tax_total) AS _amount
      FROM
      purchase_purchase_order AS _order
      JOIN
      auth_user AS _user
      ON _user.id = :__user__
      AND _order.company = _user.active_company
      WHERE
      DATE(_order.validation_date_time)
      BETWEEN DATE(:fromDate) AND DATE(:toDate)
      AND
      _order.status_select IN (4,5)
      GROUP BY
      _month
      ORDER
      BY
      _month
    </dataset>
    <category key="_month" type="month" title="Month"/>
    <series key="_amount" type="bar" title="Amount"/>
    <config name="xFormat" value="MM-YYYY"/>
  </chart>

  <chart onInit="action.purchase.chart.set.date" name="chart.total.purchase.qty"
    title="Total Purchased Qty by month">
    <search-fields>
      <field type="date" name="fromDate" title="From Date" x-required="true"/>
      <field name="monthSelect" selection="base.month.select" title="Historical Period"
        x-required="true"/>
    </search-fields>
    <dataset type="sql">
      SELECT
      to_char(_order.validation_date_time,'yyyy-MM') AS _month,
      SUM(_line.qty)
      AS _qty
      FROM
      purchase_purchase_order AS _order
      JOIN
      auth_user AS _user
      ON _user.id =
      :__user__
      AND
      _order.company = _user.active_company
      JOIN
      purchase_purchase_order_line AS _line
      ON
      _order.id =
      _line.purchase_order
      WHERE
      DATE(_order.validation_date_time) &gt;= DATE(:fromDate)
      AND
      DATE(_order.validation_date_time) &lt;= DATE(:fromDate) + CAST(:monthSelect as int) *
      INTERVAL '1 month'
      AND _order.status_select IN (4,5)
      GROUP BY
      _month
      ORDER BY
      _month
    </dataset>
    <category key="_month" type="month" title="Month"/>
    <series key="_qty" type="bar" title="Units"/>
    <config name="xFormat" value="MM-YYYY"/>
  </chart>

  <chart onInit="action.purchase.chart.set.date.6m" name="chart.nbr.of.po.per.month"
    title="Nbr of PO by month">
    <search-fields>
      <field type="date" name="fromDate" title="From Date" x-required="true"/>
      <field type="date" name="toDate" title="To Date" x-required="true"/>
    </search-fields>
    <dataset type="sql">
      SELECT
      COUNT(_order.id) as _orders,
      to_char(_order.validation_date_time,'yyyy-MM') AS _month
      FROM
      purchase_purchase_order AS _order
      JOIN
      auth_user AS _user
      ON _user.id = :__user__
      AND _order.company = _user.active_company
      WHERE
      DATE(_order.validation_date_time) BETWEEN DATE(:fromDate) AND DATE(:toDate)
      AND
      _order.status_select IN
      (4,5)
      GROUP BY
      _month
      ORDER BY
      _month
    </dataset>
    <category key="_month" type="month" title="Month"/>
    <series key="_orders" type="bar" title="ChartPO.saleOrder"/>
    <config name="xFormat" value="MM-YYYY"/>
  </chart>

  <chart onInit="action.purchase.chart.set.date.6m"
    name="chart.purchase.amount.per.product.family"
    title="Purchased amount by product accounting family">
    <search-fields>
      <field type="date" name="fromDate" title="From Date" x-required="true"/>
      <field type="date" name="toDate" title="To Date" x-required="true"/>
    </search-fields>
    <dataset type="jpql">
      SELECT
      SUM(self.exTaxTotal) AS amount,
      product.productFamily.name AS
      _family_product
      FROM
      PurchaseOrder self,
      PurchaseOrderLine line,
      Product product

      WHERE
      line.purchaseOrder = self
      AND line.product = product
      AND DATE(self.validationDateTime) &gt;=
      DATE(:fromDate)
      AND
      DATE(self.validationDateTime) &lt;= DATE(:toDate)
      AND self.createdOn &lt;=
      CURRENT_TIMESTAMP
      AND
      self.statusSelect &gt; 3
      AND self.statusSelect &lt; 6
      AND self.company =
      :activeCompany
      GROUP BY
      product.productFamily.name
      ORDER BY
      product.productFamily.name
    </dataset>
    <category key="_family_product" type="text" title="Product Accounting Family"/>
    <series key="amount" type="bar" title="Amount"/>
  </chart>

  <custom name="call-tender-product-attributes-by-suppliers"
    title="Product attributes by supplier">
    <dataset type="sql">
      <![CDATA[
        SELECT
          product.name AS product,
          partner.simple_full_name AS supplier,
          ROUND(COALESCE(product.sale_price, offer.proposed_price)) AS unit_price,
          COALESCE(requested_unit.name, proposed_unit.name) AS unit,
          ROUND(COALESCE(need.requested_qty, offer.proposed_qty)) AS qty,
          COALESCE(need.requested_date, offer.requested_date) + COALESCE(need.requested_delivery_time, offer.requested_delivery_time) AS delivery_time
        FROM purchase_call_tender AS tender
          LEFT JOIN purchase_call_tender_need AS need ON need.call_tender = tender.id
          LEFT JOIN base_product AS product ON product.id = need.product
          LEFT JOIN purchase_call_tender_supplier AS tender_supplier ON tender_supplier.call_tender = tender.id
          LEFT JOIN base_partner AS partner ON partner.id = tender_supplier.supplier_partner
          LEFT JOIN purchase_call_tender_offer AS offer ON offer.call_tender = tender.id AND offer.supplier_partner = partner.id AND offer.product = product.id
          LEFT JOIN base_unit AS proposed_unit ON proposed_unit.id = offer.proposed_unit
          LEFT JOIN base_unit AS requested_unit ON requested_unit.id = need.unit
        WHERE tender.id = :_callTenderId
        ORDER BY product.id, partner.id
      ]]>
    </dataset>
    <template>
      <![CDATA[
        <>
          <Box p={2} w={100}>
            {data.filter((row, idx, arr) => row.product && (idx === 0 || arr[idx-1].product !== row.product)).map((productRow, pi) => {
              const productRows = data.filter(r => r.product === productRow.product && r.supplier);
              return (
                <Box key={pi} mb={3} style={{ borderRadius: "12px", boxShadow: "0 2px 12px rgba(0,0,0,0.10)", overflow: "hidden", border: "1px solid #e8e8e8" }}>
                  <Box fontWeight="bold" style={{ backgroundColor: "#1A2980", color: "white", padding: "12px 16px", fontSize: "14px", letterSpacing: "0.3px" }}>
                    {productRow.product}
                  </Box>
                  <Box d="flex" style={{ backgroundColor: "#f5f5f5", padding: "8px 10px", gap: "6px" }}>
                    <Box flex="1" style={{ backgroundColor: "#8A97A8", color: "white", borderRadius: "20px", padding: "5px 10px", textAlign: "center", fontWeight: "600", fontSize: "12px" }}>{_t('Supplier')}</Box>
                    <Box flex="1" style={{ backgroundColor: "#8A97A8", color: "white", borderRadius: "20px", padding: "5px 10px", textAlign: "center", fontWeight: "600", fontSize: "12px" }}>{_t('Unit price')}</Box>
                    <Box flex="1" style={{ backgroundColor: "#8A97A8", color: "white", borderRadius: "20px", padding: "5px 10px", textAlign: "center", fontWeight: "600", fontSize: "12px" }}>{_t('Unit')}</Box>
                    <Box flex="1" style={{ backgroundColor: "#8A97A8", color: "white", borderRadius: "20px", padding: "5px 10px", textAlign: "center", fontWeight: "600", fontSize: "12px" }}>{_t('Qty')}</Box>
                    <Box flex="1" style={{ backgroundColor: "#8A97A8", color: "white", borderRadius: "20px", padding: "5px 10px", textAlign: "center", fontWeight: "600", fontSize: "12px" }}>{_t('Delivery time')}</Box>
                  </Box>
                  {productRows.map((row, ri) => (
                    <Box key={ri} d="flex" style={{ backgroundColor: ri % 2 === 0 ? "#F6F2FF" : "#FFFFFF", borderTop: "1px solid #eeeeee" }}>
                      <Box flex="1" p={2} style={{ borderRight: "1px solid #eeeeee" }}>{row.supplier}</Box>
                      <Box flex="1" p={2} style={{ borderRight: "1px solid #eeeeee" }}>{row.unit_price}</Box>
                      <Box flex="1" p={2} style={{ borderRight: "1px solid #eeeeee" }}>{row.unit}</Box>
                      <Box flex="1" p={2} style={{ borderRight: "1px solid #eeeeee" }}>{row.qty}</Box>
                      <Box flex="1" p={2}>{row.delivery_time}</Box>
                    </Box>
                  ))}
                </Box>
              );
            })}
          </Box>
        </>
      ]]>
    </template>
  </custom>

  <custom name="call-tender-supplier-respond-by-product"
    title="Suppliers' respond by product">
    <dataset type="sql">
      <![CDATA[
        SELECT
          product.id AS product_id,
          product.name AS product_name,
          ROUND(product.sale_price) AS requested_price,
          requested_unit.name AS requested_unit,
          ROUND(need.requested_qty) AS requested_qty,
          need.requested_date + need.requested_delivery_time AS requested_delivery_date,
          partner.id AS supplier_id,
          partner.simple_full_name AS supplier_name,
          ROUND(offer.proposed_price) AS proposed_price,
          proposed_unit.name AS proposed_unit,
          ROUND(offer.proposed_qty) AS proposed_qty,
          offer.requested_date + offer.requested_delivery_time AS proposed_delivery_date
        FROM purchase_call_tender AS tender
          LEFT JOIN purchase_call_tender_need AS need ON need.call_tender = tender.id
          LEFT JOIN base_product AS product ON product.id = need.product
          LEFT JOIN purchase_call_tender_supplier AS tender_supplier ON tender_supplier.call_tender = tender.id
          LEFT JOIN base_partner AS partner ON partner.id = tender_supplier.supplier_partner
          LEFT JOIN purchase_call_tender_offer AS offer ON offer.call_tender = tender.id AND offer.supplier_partner = partner.id AND offer.product = product.id
          LEFT JOIN base_unit AS requested_unit ON requested_unit.id = need.unit
          LEFT JOIN base_unit AS proposed_unit ON proposed_unit.id = offer.proposed_unit
        WHERE tender.id = :_callTenderId
        ORDER BY product.id, partner.id
      ]]>
    </dataset>
    <template>
      <![CDATA[
        <>
          <Box p={2} w={100} style={{ overflowX: "auto" }}>
            {data.length > 0 && (() => {
              const supplierColors = ["#7B6FE0", "#4A9AE0"];
              const supplierDataColors = ["#F6F2FF", "#F0F7FF"];
              const getSupplierColor = (index) => supplierColors[index % 2];
              const getSupplierDataColor = (index) => supplierDataColors[index % 2];
              const borderColor = "#d0d0d0";
              const pill = (bg) => ({ backgroundColor: bg, color: "white", borderRadius: "20px", padding: "4px 10px", display: "inline-block", fontWeight: "600", fontSize: "12px", whiteSpace: "nowrap" });
              const suppliers = data.filter(
                (row, idx, arr) =>
                  row.supplier_id && arr.findIndex(s => s.supplier_id === row.supplier_id) === idx
              );
              const products = data.filter(
                (row, idx, arr) =>
                  row.product_id && arr.findIndex(p => p.product_id === row.product_id) === idx
              );

              return (
                <div style={{ borderRadius: "12px", boxShadow: "0 2px 12px rgba(0,0,0,0.10)", border: "1px solid #e8e8e8" }}>
                  <table style={{ borderCollapse: "collapse", tableLayout: "auto", whiteSpace: "nowrap" }}>
                    <thead>
                      <tr>
                        <th style={{ border: `1px solid ${borderColor}`, minWidth: "140px", backgroundColor: "#f5f5f5" }}></th>
                        <th colSpan={4} style={{ border: `1px solid ${borderColor}`, backgroundColor: "#29D4E4", color: "white", padding: "10px 8px", textAlign: "center", fontWeight: "bold", fontSize: "13px" }}>
                          {_t('Requested')}
                        </th>
                        {suppliers.map((supplier, si) => (
                          <th key={supplier.supplier_id} colSpan={4} style={{ border: `1px solid ${borderColor}`, backgroundColor: getSupplierColor(si), color: "white", padding: "10px 8px", textAlign: "center", fontWeight: "bold", fontSize: "13px" }}>
                            {supplier.supplier_name}
                          </th>
                        ))}
                      </tr>
                      <tr>
                        <th style={{ border: `1px solid ${borderColor}`, minWidth: "140px", backgroundColor: "#f5f5f5" }}></th>
                        <th style={{ border: `1px solid ${borderColor}`, backgroundColor: "#e8fbfd", padding: "6px 4px", textAlign: "center" }}><div style={pill("#29D4E4")}>{_t('Unit price')}</div></th>
                        <th style={{ border: `1px solid ${borderColor}`, backgroundColor: "#e8fbfd", padding: "6px 4px", textAlign: "center" }}><div style={pill("#29D4E4")}>{_t('Unit')}</div></th>
                        <th style={{ border: `1px solid ${borderColor}`, backgroundColor: "#e8fbfd", padding: "6px 4px", textAlign: "center" }}><div style={pill("#29D4E4")}>{_t('Qty')}</div></th>
                        <th style={{ border: `1px solid ${borderColor}`, backgroundColor: "#e8fbfd", padding: "6px 4px", textAlign: "center" }}><div style={pill("#29D4E4")}>{_t('Delivery time')}</div></th>
                        {suppliers.map((supplier, si) => (
                          <React.Fragment key={`${supplier.supplier_id}-header`}>
                            <th style={{ border: `1px solid ${borderColor}`, backgroundColor: si % 2 === 0 ? "#F0EEFF" : "#EAF4FF", padding: "6px 4px", textAlign: "center" }}><div style={pill(getSupplierColor(si))}>{_t('Unit price')}</div></th>
                            <th style={{ border: `1px solid ${borderColor}`, backgroundColor: si % 2 === 0 ? "#F0EEFF" : "#EAF4FF", padding: "6px 4px", textAlign: "center" }}><div style={pill(getSupplierColor(si))}>{_t('Unit')}</div></th>
                            <th style={{ border: `1px solid ${borderColor}`, backgroundColor: si % 2 === 0 ? "#F0EEFF" : "#EAF4FF", padding: "6px 4px", textAlign: "center" }}><div style={pill(getSupplierColor(si))}>{_t('Qty')}</div></th>
                            <th style={{ border: `1px solid ${borderColor}`, backgroundColor: si % 2 === 0 ? "#F0EEFF" : "#EAF4FF", padding: "6px 4px", textAlign: "center" }}><div style={pill(getSupplierColor(si))}>{_t('Delivery time')}</div></th>
                          </React.Fragment>
                        ))}
                      </tr>
                    </thead>
                    <tbody>
                      {products.map((productRow) => (
                        <tr key={productRow.product_id}>
                          <td style={{ border: `1px solid ${borderColor}`, padding: "8px", fontWeight: "bold", minWidth: "140px", backgroundColor: "#00BCD4", color: "white" }}>
                            {productRow.product_name}
                          </td>
                          <td style={{ border: `1px solid ${borderColor}`, backgroundColor: "#EDFCFE", padding: "8px" }}>{productRow.requested_price}</td>
                          <td style={{ border: `1px solid ${borderColor}`, backgroundColor: "#EDFCFE", padding: "8px" }}>{productRow.requested_unit}</td>
                          <td style={{ border: `1px solid ${borderColor}`, backgroundColor: "#EDFCFE", padding: "8px" }}>{productRow.requested_qty}</td>
                          <td style={{ border: `1px solid ${borderColor}`, backgroundColor: "#EDFCFE", padding: "8px" }}>
                            {productRow.requested_delivery_date ? $moment(productRow.requested_delivery_date).format('DD/MM/YYYY') : ''}
                          </td>
                          {suppliers.map((supplier, si) => {
                            const offer = data.find(
                              (row) =>
                                row.product_id === productRow.product_id &&
                                row.supplier_id === supplier.supplier_id
                            ) || {};
                            return (
                              <React.Fragment key={`${productRow.product_id}-${supplier.supplier_id}`}>
                                <td style={{ border: `1px solid ${borderColor}`, backgroundColor: getSupplierDataColor(si), padding: "8px" }}>{offer.proposed_price}</td>
                                <td style={{ border: `1px solid ${borderColor}`, backgroundColor: getSupplierDataColor(si), padding: "8px" }}>{offer.proposed_unit}</td>
                                <td style={{ border: `1px solid ${borderColor}`, backgroundColor: getSupplierDataColor(si), padding: "8px" }}>{offer.proposed_qty}</td>
                                <td style={{ border: `1px solid ${borderColor}`, backgroundColor: getSupplierDataColor(si), padding: "8px" }}>
                                  {offer.proposed_delivery_date ? $moment(offer.proposed_delivery_date).format('DD/MM/YYYY') : ''}
                                </td>
                              </React.Fragment>
                            );
                          })}
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              );
            })()}
          </Box>
        </>
      ]]>
    </template>
  </custom>

</object-views>
