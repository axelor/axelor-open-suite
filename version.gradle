ext.versionFile = "modules/abs/version.txt"
ext.baseVersion = rootProject.file(versionFile).text.trim()

version baseVersion

if(project.hasProperty("mavenRepository") && !project.hasProperty("finalRelease")) {
  version += "-SNAPSHOT"
}

ext.absVersion = version
