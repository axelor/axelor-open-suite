<?xml version="1.0" encoding="UTF-8"?>
<domain-models xmlns="http://apps.axelor.com/xml/ns/domain-models"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://apps.axelor.com/xml/ns/domain-models http://apps.axelor.com/xml/ns/domain-models/domain-models_0.9.xsd">

  <module name="base" package="com.axelor.apps.base.db"/>

  <entity name="BankDetails" lang="java">
  
  	<integer name="importId"/>
    <many-to-one name="partner" ref="Partner" title="tiers"/>
    <string name="ownerName" title="Owner name" max="255" help="Name of the bank account owner"/>
    <string name="bankAddress" title="SWIFT address" max="255" help="Address of the bank"/>
    <string name="bic" title="BIC (Bank Identifier)"/>
    <string name="iban" title="IBAN" help="For more information regarding IBAN, please refer to the norm ISO13616 (a sum up is introduced here http://fr.wikipedia.org/wiki/ISO_13616)"/>
    <string name="bankCode" title="Bank Code" max="255" help="Refers to the bank code where the account owner has his account."/>
    <string name="sortCode" title="Code Guichet" max="255" help="Refers to the branch code where the account is registered."/>
    <string name="accountNbr" title="Account Number" max="255" />
    <string name="bbanKey" title="BBan Key" max="255" help=""/>
    <string name="countryCode" title="Country Code" max="255" help="Refers to the country code expressed within the first 2 caracters of the IBAN."/>
    <string name="ibanBic" title="IBAN + BIC" max="255"/>
    <string name="fullName" title="Bank Details" search="ownerName" namecolumn="true">
        <![CDATA[
            String str = "";
            if (!(ownerName == null || ownerName.isEmpty())) {
                str = ownerName;
                if (!(ibanBic == null || ibanBic.isEmpty())) {
                    str = str.concat(" - ").concat(ibanBic);
                } else if (!(iban == null || iban.isEmpty())) {
                    str = str.concat(" - ").concat(iban);
                } else if (!(bankCode == null || bankCode.isEmpty())
                        && !(sortCode == null || sortCode.isEmpty())
                        && !(accountNbr == null || accountNbr.isEmpty())
                        && !(bbanKey == null || bbanKey.isEmpty())) {
                    str = str.concat(" - ").concat(bankCode)
                                           .concat(sortCode)
                                           .concat(accountNbr)
                                           .concat(bbanKey);
                }
            }
            return str;
        ]]>
    </string>

  </entity>

  <!--
  
  Coordonnées bancaires
  
  -->
</domain-models>
