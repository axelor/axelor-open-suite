<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<object-views xmlns="http://apps.axelor.com/xml/ns/object-views"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://apps.axelor.com/xml/ns/object-views http://apps.axelor.com/xml/ns/object-views/object-views_0.9.xsd">
    
    <grid name="template-grid" title="Templates" model="com.axelor.apps.base.db.Template">
        <field name="name"/>
        <field name="subject"/>
        <field name="target"/>
        <field name="metaModel"/>
    </grid>
    
    <form name="template-form" title="Template" model="com.axelor.apps.base.db.Template" cols="4"
    	onNew="action-template-record-new">
        <field name="name"/>
        <field name="metaModel"/>
        <field name="target"/>
        <field name="birtTemplate"/>
        <break/>
        <field name="subject" colSpan="4" widget="Html"/>
        <field name="content" colSpan="4" widget="Html"/>
        <break/>
        <field name="templateContext" colSpan="4"/>
    </form>
    
    <action-record name="action-template-record-new" model="com.axelor.apps.base.db.Template">
    	<field name="metaModel" expr="eval: _parent?.targetSelect == 1 ? com.axelor.meta.db.MetaModel.all().filter('self.name = \'Partner\'').fetchOne() : (_parent?.targetSelect == 2 ? com.axelor.meta.db.MetaModel.all().filter('self.name = \'Lead\'').fetchOne() : null)" if="_parent?.targetSelect"/>
    	<field name="metaModel" expr="eval: _parent?.metaModel" if="_parent?.metaModel"/>
    	<field name="metaModel" expr="eval: _parent?._parent?.metaModel" if="_parent?._parent?.metaModel"/>
    </action-record>
    
</object-views>