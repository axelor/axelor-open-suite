<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<object-views xmlns="http://apps.axelor.com/xml/ns/object-views"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://apps.axelor.com/xml/ns/object-views http://apps.axelor.com/xml/ns/object-views/object-views_2.0.xsd">
	
    <grid name="mail-account-grid" title="Mail accounts" model="com.axelor.apps.message.db.MailAccount">
        <field name="name"/>
        <field name="login"/>
        <field name="host"/>
        <field name="isDefault"/>
    </grid>
    
    <form name="mail-account-form" title="Mail account" model="com.axelor.apps.message.db.MailAccount" cols="4" 
    onNew="action-mail-account-default-record">
    	<group title="Informations" colSpan="4">
            <field name="name"/>
            <field name="user"/>
        	<field name="signature" widget="Html" x-lite="true" colSpan="4"/>
        	<field name="isDefault" onChange="action-mail-account-validate-is-default"/>
        </group>
        <group title="Login" colSpan="4">
       	 	<field name="login"/>
        	<field name="password" widget="password"/>
        </group>
        <group title="Configurations" colSpan="4">
        	<field name="serverTypeSelect"/>
        	<field name="host"/>
        	<field name="port"/>
        	<field name="securitySelect"/>
        </group>
    </form>

	<action-record name="action-mail-account-default-record" model="com.axelor.apps.message.db.MailAccount">
    	<field name="user" expr="eval:__user__"/>
    </action-record>
    
	<action-validate name="action-mail-account-validate-is-default">
		<error message="There is already a default mail account" if="com.axelor.apps.message.db.MailAccount.all().filter('self.isDefault = true and self.user = :user').count() > 0 &amp;&amp; isDefault = true"/>
	</action-validate>
    
</object-views>
